// import { DbDriver } from '../modules/database';

// const db = DbDriver.getInstance().db;

// export function userHasApplication(user: string) {
// 	const query = db.query('SELECT * FROM applications WHERE userId = ?');
// 	return query.get(user);
// }
